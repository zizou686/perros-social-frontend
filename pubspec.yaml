name: perros_app
description: "A new Flutter project."
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: ^3.11.5

dependencies:
  flutter:
    sdk: flutter
  image_picker: ^1.1.0
  cupertino_icons: ^1.0.8
  http: ^1.1.0
  http_parser: ^4.0.0
  shared_preferences: ^2.2.0
  # ✅ Cliente STOMP/WebSocket para Flutter
  stomp_dart_client: ^1.4.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
